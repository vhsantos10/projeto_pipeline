pipeline {
    agent any
    
    environment { 
        CI = 'true'
    }

    stages {
        stage('Build') {
            steps {
                echo 'Clonando o repositório...'
            }
        }

        stage('Teste') {
            steps {
                echo 'Clonando o repositório...'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Publicando index.html no servidor...'
                
            }
        }
    }
}
